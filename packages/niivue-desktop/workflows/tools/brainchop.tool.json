{
  "$schema": "../schemas/tool.schema.json",
  "name": "brainchop",
  "version": "1.0.0",
  "description": "Skull strip a NIfTI volume using Brainchop MindGrab (in-browser WebGL inference)",
  "inputs": {
    "nifti_paths": {
      "type": "volume[]",
      "description": "Anatomical volumes to skull strip"
    },
    "nifti_path": {
      "type": "volume",
      "description": "Per-iteration anatomical volume (set by forEach)",
      "optional": true
    },
    "output_path": {
      "type": "string",
      "description": "Path for the skull-stripped output NIfTI",
      "optional": true
    },
    "model": {
      "type": "string",
      "description": "Brainchop model to use for brain extraction",
      "optional": true,
      "default": "brain-extract-mindgrab"
    },
    "dilation": {
      "type": "number",
      "description": "Brain-mask dilation in mm (volume is 1mm isotropic, so 1 mm = 1 voxel)",
      "optional": true,
      "default": 3,
      "min": 0,
      "max": 10
    }
  },
  "outputs": {
    "output_paths": {
      "type": "volume[]",
      "description": "Paths to the skull-stripped NIfTI volumes"
    }
  },
  "block": {
    "id": "skull-strip",
    "label": "Skull Strip",
    "description": "Remove non-brain tissue using ML-based extraction",
    "category": "Processing",
    "icon": "ScissorsIcon",
    "defaults": {
      "model": "brain-extract-mindgrab"
    },
    "exposedFields": ["dilation"],
    "hiddenFields": ["nifti_paths", "nifti_path", "model", "output_path"],
    "requiredContextFields": ["series_list", "subjects"],
    "contextFields": {
      "series_list": {
        "type": "series-mapping[]",
        "label": "Series Classification",
        "description": "BIDS-classified series mappings (drives the skull-strip editor's per-image table)",
        "heuristic": "bids-classify"
      },
      "subjects": {
        "type": "subject[]",
        "label": "Subjects",
        "description": "Detected subjects and sessions grouped by PatientID and AcquisitionDate",
        "heuristic": "detect-subjects"
      },
      "dilation": {
        "type": "number",
        "label": "Mask dilation (mm)",
        "description": "Brain-mask dilation in mm (volume is conformed to 1mm isotropic, so 1 mm = 1 voxel)",
        "default": 3,
        "min": 0,
        "max": 10
      }
    },
    "formComponent": "skull-strip-editor"
  }
}
